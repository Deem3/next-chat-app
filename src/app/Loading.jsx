'use client'
// import { useAuthState } from 'react-firebase-hooks/auth'
// import { auth } from '@/pages/api/firebase'

export default function Loading() {
    // const [user, loading, error] = useAuthState(auth)

        return (
            <div>loadingasdsds</div>
          )
}
